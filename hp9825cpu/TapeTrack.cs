namespace HP9825CPU
{
    /// <summary>
    /// Indicates the tape track as enumeration (A = 0, B = 1)
    /// </summary>
    public enum TapeTrack
    {
        /// <summary>
        /// The first (index 0) tape track.
        /// </summary>
        A = 0,
        /// <summary>
        /// The second (index 1) tape track.
        /// </summary>
        B = 1
    }
}